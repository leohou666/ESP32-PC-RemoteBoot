cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(remote_boot)

# Create SPIFFS image from web/ directory, flash alongside the app
spiffs_create_partition_image(spiffs main/web FLASH_IN_PROJECT)
